function             % phrase ASE if you mean "functionality", then use "functionality" because function can mean C function!
functions            % phrase ASE if you mean "functionalities", then use "functionalities" because functions can mean C functions!
#method               % phrase ASE if you mean "technique", then use "technique" because method can mean Java method!
#methods              % phrase ASE if you mean "techniques", then use "techniques" because methods can mean Java methods!
check if             % phrase ASE if you mean "check whether", then use "check whether" because if can mean when!
